/** Seconds shown in the session-expiry toast before forced logout. */
export const SESSION_EXPIRY_WARNING_SECONDS = 30;

/** Fired by the API client when an authenticated request returns 401. */
export const AUTH_UNAUTHORIZED_EVENT = 'auth:unauthorized';
